spring:
  application:
    name: rainCity-hazard-detector

server:
  port: 8080

app:
  supabase:
    url: https://pmfcphaxxcsovsuubqgu.supabase.co
    # Accessing the REST API interface of Supabase
    rest-url: ${app.supabase.url}/rest/v1
    anon-key: sb_publishable_3wFlYvfiZlLQOein07n37Q_hjse-w3Y
  
  ai-model:
    # Based on your HF Space: https://huggingface.co/spaces/sdl11/intersection-hazard-api
    # If using Gradio Client, the endpoint often looks like this. 
    # If it is a direct API, replace with the specific endpoint provided via the "Use via API" button on HF.
    hf-api-url: https://sdl11-intersection-hazard-api.hf.space/api/predict
    hf-token: hf_YOUR_HUGGINGFACE_TOKEN 
  
  deepseek-api:
    url: https://deepseekv32-3ca9s.paas.ai.telus.com
    access-token: a12a7d3705b12aeb46eb4cc8d77f5446

# Scheduling settings (e.g., check cameras every 10 seconds)
hazard:
  refresh-rate-ms: 10000
